document.addEventListener('DOMContentLoaded', (event) => {
    document.getElementById('backToMapButtonStory').addEventListener('click', function() {
        // Navigate to the adventureMap html
        window.location.href = '/adventureMap';
    });
});
